{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build-dev": {
      "dependsOn": ["^build"]
    },
    "bundle": {
      "dependsOn": ["^bundle"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    }
  }
}
