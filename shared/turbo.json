{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "bundle": {
      "dependsOn": ["@tunarr/types#build"],
      "outputs": ["build/**"]
    },
    "build": {
      "dependsOn": ["@tunarr/types#build"],
      "outputs": ["build/**"]
    },
    "build-dev": {
      "cache": false
    }
  }
}
